/* Включаем заголовочный файл с описанием функции hello() */
#include "hello.h"

int main(int argc, char **argv) {
  /* Вызываем функцию hello(), определенную в другом файле */ 
  hello();
  return 0; 
}